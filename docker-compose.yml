version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    # image: 'aakarshak1/react_project'
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
